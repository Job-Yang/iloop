# 贡献指南

欢迎为 iLoop 贡献。先读 [DESIGN.md](DESIGN.md) 理解架构，再动手。

## 第一性原则（比任何规则都高）

iLoop 自己也遵守 VDD：**改了代码，必须先自证一遍。**

- **改内核/插件/协议，完成前必须 `python3 -m cli selftest` 全绿。** 只测"能生成产物"不算，要测"结论对"。规则改了让代码/测试跟上，别只写文档不验证。
- **推断不许当观测**：PR 描述里写清哪些是你真跑验过的、哪些是推断。

## 核心 vs 扩展：先分清你在改哪层

- **公共核心**（`kernel/`、`plugins/ios_native/`、协议、提示词）：服务所有 iLoop 用户、必须进主仓的能力。改这里走本指南的 PR 流程。
- **业务扩展**（只服务某团队/业务的 flow、领域插件、私有平台适配）：**不要改核心**，走 `python3 -m cli extension-init`，只改你的扩展目录。见 [EXTENDING.md](EXTENDING.md)。

判断法：**"换一家公司、换一套工具链，这段逻辑还成立吗？"** 成立才进核心，不成立做成扩展/插件。

## 核心的硬边界

- **内核零平台绑定**：`kernel/` 不许出现任何具体平台的 CLI / 域名 / 字段（selftest 有红线断言拦截）。平台通过实现 `Capability` / `EventSource` / `Notifier` 接口插进来。
- **内核零第三方依赖**：只用 Python 标准库。加依赖前先问：真的必要吗？
- **诚实缺口**：能力没实现就返回 `unsupported` 或在 `KNOWN_GAPS` 标注，绝不假装完整。

## PR 流程

1. Fork + 建分支。
2. 改代码 + 补/改 selftest 断言（新能力必须有断言覆盖）。
3. `python3 -m cli selftest` 全绿。
4. 提 PR，描述写清：改了什么、为什么、怎么验的（贴 selftest 结果）。

## 代码风格

- 跟随既有风格（命名、注释密度、中文注释惯例），不顺手重构无关代码。
- 最小改动，不加投机抽象。VDD 反对"补丁摞补丁"，改动要能通过全局回看。
